<section class="main-content-section main-content-section-team">
    <div class="main-content-container">
        <div class="bp-section section-team">
            <div class="container">
                {% if content.field_title|render|trim is not empty %}
                    <h2 class="section-title">{{ content.field_title }}</h2>
                {% endif %}

                {{ content.field_personnel_list }}
            </div>
        </div>
    </div>
</section>